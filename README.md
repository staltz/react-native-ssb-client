# Scuttlebot Client for React Native apps

## install

Install with `npm install`, *not* Yarn.

`npm install --save react-native-ssb-client`

## usage

Same as [`ssb-client`](https://www.npmjs.com/package/ssb-client).
